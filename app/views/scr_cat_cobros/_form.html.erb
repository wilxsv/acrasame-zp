<%= form_for(@scr_cat_cobro) do |f| %>
  <% if @scr_cat_cobro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scr_cat_cobro.errors.count, "error") %> prohibited this scr_cat_cobro from being saved:</h2>

      <ul>
      <% @scr_cat_cobro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :Nombre %><br>
    <%= f.text_field :cCobroNombre %>
  </div>
  <div class="control-group">
    <%= f.label :Descripción %><br>
    <%= f.text_area :cCobroDescripcion %>
  </div>
  <div class="actions">
    <%= f.submit "Enviar", class:"btn btn-mini btn-primary", data: { confirm: 'Confirma la operación ?' } %>
  </div>
<% end %>
