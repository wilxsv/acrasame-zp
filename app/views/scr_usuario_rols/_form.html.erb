<%= form_for(@scr_usuario_rol) do |f| %>
  <% if @scr_usuario_rol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scr_usuario_rol.errors.count, "error") %> prohibited this scr_usuario_rol from being saved:</h2>

      <ul>
      <% @scr_usuario_rol.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :usuario_id %><br>
    <%= f.number_field :usuario_id %>
  </div>
  <div class="field">
    <%= f.label :rol_id %><br>
    <%= f.number_field :rol_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
