<div class="module">
 <div class="module-head">
  <h3>Cuentas donde se aplicara el cobro automatico en el pago de recibos</h3>
 </div>
 <div class="module-body">
  <%= form_for :transacx, class:"form-horizontal row-fluid", :url => { :action => :pago } do |f| %>
   <div class="control-group">
    <%= f.label :cuenta_debe %>
    <div class="controls">
     <div class="input-append">
      <%= f.select :pdebe, ScrCuentua.order('CAST("cuentaCodigo" AS TEXT)').all.collect { |p| [ p.cuentaCodigo.to_s+"-"+p.cuentaNombre, p.id ] }, include_blank: false %>
     </div>
    </div>
   </div>
   <div class="control-group">
    <%= f.label :cuenta_haber %>
    <div class="controls">
     <div class="input-append">
      <%= f.select :phaber, ScrCuentua.order('CAST("cuentaCodigo" AS TEXT)').all.collect { |p| [ p.cuentaCodigo.to_s+"-"+p.cuentaNombre, p.id ] }, include_blank: false %>
     </div>
    </div>
   </div>
   <%= f.submit "Automatizar pagos", class:"btn btn-mini btn-primary", data: { confirm: 'Confirma la operación ?' } %>
  <% end %>
 </div>
</div>
<br />
