<%= form_for(@scr_lectura) do |f| %>
  <% if @scr_lectura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scr_lectura.errors.count, "error") %> prohibited this scr_lectura from being saved:</h2>

      <ul>
      <% @scr_lectura.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    Socio: <%= f.select :socio_id, ScrUsuario.order(:nombreusuario).all.collect { |p| [ p.nombreusuario+"  ".html_safe+p.apellidousuario, p.id ] }, include_blank: false %><br/>
    Empleado: <%= f.select :tecnico_id, ScrEmpleado.order(:empleadoNombre).all.collect { |p| [ p.empleadoNombre+"  ".html_safe+p.empleadoApellido, p.id ] }, include_blank: false %><br/>
    Fecha: <%= f.text_field :fechaLectura, placeholder:Time.now.strftime("%Y-%d-%m"), id: "datepicker" %><br/>
    Lectura: <%= f.text_field :valorLectura, placeholder:"000" %><span class="add-on">#</span>
  </div>
  <div class="actions">
    <%= f.submit "Enviar", class:"btn btn-mini btn-primary", data: { confirm: 'Confirma la operación ?' } %>
  </div>
<% end %>
