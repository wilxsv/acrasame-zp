<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Cuenta</th>
      <th>Nombre</th>
      <th>Debe</th>
      <th>Haber</th>
    </tr>
  </thead>

  <tbody>
   <% if session['html'] %>
    <%= session['html'].html_safe || "" %>
   <tr><td colspan="2"></td><td><b><%= session['debe'] %></b></td><td><b><%= session['haber'] %></b></td></tr>
   <tr><td>Fecha</td><td colspan="3"><%= session['fecha'] %></td></tr>
   <% end %>
  </tbody>
</table>
<%= form_for :transacx, :url => { :action => :create } do |f| %>
  <%= f.submit "Finalizar transacción", class: "btn-success btn-sm" %>
<% end %>
